# this file overrides all pipeline specs in any sub-directories
# you have to match the relative pipeline id (in this case - load)

load:
  pipeline:
    # disables any dump to sql / elasticsearch steps
    - run: plus_plus.dump_to_sql
      disable: true
    - run: plus_plus.dump_to_elasticsearch
      disable: true
